import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      marginTop: 50,
      flex: 1,
      alignItems: 'center',
    },
    item: {
      marginVertical: 8,
      flex: 1,
      flexDirection: 'row',
      alignItems: 'center'
    },
    contentContainer: {
      paddingBottom: 150
    }
  });

export default styles;